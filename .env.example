### Example environment variables for docker and CI
# Replace placeholders with your real values before use

# Docker Hub
DOCKERHUB_ACCOUNT=your-dockerhub-username
REPO_PREFIX=acompanamiento
TAG=latest

# Node environment
NODE_ENV=production

# Service ports (can be overridden in compose)
MICRO_AUTH_PORT=5005
API_GATEWAY_PORT=8080
FRONTEND_PORT=5500

# Database URLs (production should use managed services)
MONGO_URL=mongodb://mongo:27017/acompanamiento
REDIS_URL=redis://redis:6379

# JWT settings (example)
JWT_SECRET=replace_this_with_a_strong_secret
ACCESS_TOKEN_EXPIRES_IN=900
REFRESH_TOKEN_EXPIRES_IN=604800

# CI secrets (do NOT commit real tokens)
# In GitHub Actions, set secrets: DOCKERHUB_USERNAME, DOCKERHUB_TOKEN
# ==============================================
# CONFIGURACIÓN DE MENSAJE BROKER GLOBAL
# ==============================================

# ===== KAFKA (EventBus) =====
# Brokers de Kafka (separados por comas)
KAFKA_BROKERS=kafka:9092
# Client ID para identificar el servicio
KAFKA_CLIENT_ID=micro-estudiantes
# Log level: NOTHING(0), ERROR(1), WARN(2), INFO(3), DEBUG(4)
KAFKA_LOG_LEVEL=2

# ===== RABBITMQ (MessageQueue) =====
# URL de conexión a RabbitMQ
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672
# Máximo de conexiones simultáneas
RABBITMQ_MAX_CONNECTIONS=100
# Keepalive en segundos
RABBITMQ_HEARTBEAT=60

# ===== MQTT (Telemetría) =====
# URL del broker MQTT
MQTT_BROKER_URL=mqtt://mqtt:1883
# Client ID único
MQTT_CLIENT_ID=micro-estudiantes-telemetry
# Usuario MQTT (dejar vacío para anónimo)
MQTT_USERNAME=
# Contraseña MQTT
MQTT_PASSWORD=
# Reconectar cada N ms si se desconecta
MQTT_RECONNECT_PERIOD=1000
# Timeout de conexión
MQTT_CONNECT_TIMEOUT=10000

# ===== LOGGING =====
# Nivel de log: error, warn, info, debug
LOG_LEVEL=info
# Habilitar logs de debug
DEBUG=false

# ===== SERVICIO =====
# Nombre del microservicio
SERVICE_NAME=micro-estudiantes
# Versión del servicio
SERVICE_VERSION=1.0.0
# Entorno: development, production
NODE_ENV=development
# Puerto de la API
PORT=5002

# ===== MONGODB =====
MONGO_URI=mongodb://mongo:27017/estudiantesdb

# ===== AUTENTICACIÓN =====
JWT_SECRET=tu-secret-key-cambiar-en-produccion
JWT_EXPIRY=24h
REFRESH_SECRET=tu-refresh-secret-cambiar-en-produccion

# ===== FEATURES =====
# Habilitar EventBus
EVENTBUS_ENABLED=true
# Habilitar MessageQueue
MESSAGEQUEUE_ENABLED=true
# Habilitar Telemetría
TELEMETRY_ENABLED=true
