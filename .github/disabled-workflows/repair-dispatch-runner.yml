name: Repair dispatch runner (archived)

# Archived per request: repair workflows removed from CI.
on: []

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived
        run: echo "Original repair-dispatch-runner.yml archived and removed from active workflows"
