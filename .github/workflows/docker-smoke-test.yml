name: REMOVED: Docker Smoke Test

# Removed from active workflows per request; archived at .github/disabled-workflows/docker-smoke-test.yml
on: []

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Removed
        run: echo "Workflow removed from active workflows; archived at .github/disabled-workflows/docker-smoke-test.yml"
