name: REMOVED: Repair deploy (manual)

# Removed from active workflows per request; archived at .github/disabled-workflows/repair-deploy.yml
on: []

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Removed
        run: echo "Workflow removed from active workflows; archived at .github/disabled-workflows/repair-deploy.yml"
