name: REMOVED: CI Test for run-repair

# Removed from active workflows per request; archived at .github/disabled-workflows/ci-test-repair.yml
on: []

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Removed
        run: echo "Workflow removed from active workflows; archived at .github/disabled-workflows/ci-test-repair.yml"
