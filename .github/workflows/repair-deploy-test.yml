name: REMOVED: Repair deploy (test)

# Removed from active workflows per request; archived at .github/disabled-workflows/repair-deploy-test.yml
on: []

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Removed
        run: echo "Workflow removed from active workflows; archived at .github/disabled-workflows/repair-deploy-test.yml"
