name: 🚀 Deploy via SSH + Docker Compose (Optimized)

on:
  workflow_dispatch:
    inputs:
      rebuild_images:
        description: 'Rebuild Docker images from source'
        required: true
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'

env:
  AWS_REGION: us-east-1
  DOCKER_REGISTRY: docker.io
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
  DOCKER_PASSWORD: ${{ secrets.DOCKER_TOKEN }}

jobs:
  # =========================================================================
  # LOAD INSTANCE IPS FROM CONFIG
  # =========================================================================
  load-config:
    name: 📋 Load Configuration
    runs-on: ubuntu-latest
    outputs:
      bastion-public: ${{ steps.load.outputs.bastion_public }}
      api-gateway-public: ${{ steps.load.outputs.api_gateway_public }}
      core-public: ${{ steps.load.outputs.core_public }}
      reportes-public: ${{ steps.load.outputs.reportes_public }}
      notificaciones-public: ${{ steps.load.outputs.notificaciones_public }}
      messaging-public: ${{ steps.load.outputs.messaging_public }}
      db-public: ${{ steps.load.outputs.db_public }}
      analytics-public: ${{ steps.load.outputs.analytics_public }}
      monitoring-public: ${{ steps.load.outputs.monitoring_public }}
      frontend-public: ${{ steps.load.outputs.frontend_public }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Load instance IPs
        id: load
        run: |
          python3 << 'PYTHON'
          import json
          
          with open('config/instance_ips.json', 'r') as f:
              instances = json.load(f)
          
          mapping = {
              'EC-Bastion': 'bastion',
              'EC2-API-Gateway': 'api_gateway',
              'EC2-CORE': 'core',
              'EC2-Reportes': 'reportes',
              'EC2-Notificaciones': 'notificaciones',
              'EC2-Messaging': 'messaging',
              'EC2-DB': 'db',
              'EC2-Analytics': 'analytics',
              'EC2-Monitoring': 'monitoring',
              'EC2-Frontend': 'frontend'
          }
          
          for instance_name, output_name in mapping.items():
              if instance_name in instances:
                  public_ip = instances[instance_name]['PublicIpAddress']
                  print(f"::set-output name={output_name}_public::{public_ip}")
                  print(f"✅ {instance_name}: {public_ip}")
          PYTHON

  # =========================================================================
  # 1. DEPLOY EC2-MESSAGING (Docker Compose)
  # =========================================================================
  deploy-messaging:
    name: "⏱️  1. Deploy EC2-Messaging"
    needs: load-config
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ needs.load-config.outputs.messaging-public }} >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Deploy with Docker Compose
        run: |
          scp -i ~/.ssh/id_rsa docker-compose.messaging.yml ubuntu@${{ needs.load-config.outputs.messaging-public }}:/tmp/
          
          ssh -i ~/.ssh/id_rsa ubuntu@${{ needs.load-config.outputs.messaging-public }} << 'DEPLOY'
          set -e
          echo "================================"
          echo "⏱️  EC2-Messaging Deployment"
          echo "================================"
          
          # Instalar docker-compose si no existe
          if ! command -v docker-compose &> /dev/null; then
            echo "📦 Instalando docker-compose..."
            sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
            sudo chmod +x /usr/local/bin/docker-compose
            docker-compose --version
          fi
          
          cd /tmp
          docker-compose -f docker-compose.messaging.yml down 2>/dev/null || true
          docker-compose -f docker-compose.messaging.yml up -d
          
          sleep 15
          docker-compose -f docker-compose.messaging.yml ps
          echo "✅ EC2-Messaging deployment complete"
          DEPLOY

  # =========================================================================
  # 2. DEPLOY EC2-DB (Docker Compose)
  # =========================================================================
  deploy-db:
    name: "🗄️  2. Deploy EC2-DB"
    needs: [load-config, deploy-messaging]
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ needs.load-config.outputs.db-public }} >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Deploy with Docker Compose
        run: |
          scp -i ~/.ssh/id_rsa docker-compose.ec2-db.yml ubuntu@${{ needs.load-config.outputs.db-public }}:/tmp/docker-compose.db.yml
          
          ssh -i ~/.ssh/id_rsa ubuntu@${{ needs.load-config.outputs.db-public }} << 'DEPLOY'
          set -e
          echo "================================"
          echo "🗄️  EC2-DB Deployment"
          echo "================================"
          
          # Instalar docker-compose si no existe
          if ! command -v docker-compose &> /dev/null; then
            echo "📦 Instalando docker-compose..."
            sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
            sudo chmod +x /usr/local/bin/docker-compose
            docker-compose --version
          fi
          
          cd /tmp
          docker-compose -f docker-compose.db.yml down 2>/dev/null || true
          docker-compose -f docker-compose.db.yml up -d
          
          sleep 20
          docker-compose -f docker-compose.db.yml ps
          echo "✅ EC2-DB deployment complete"
          DEPLOY

  # =========================================================================
  # 3. DEPLOY EC2-CORE (Docker Compose)
  # =========================================================================
  deploy-core:
    name: "🐳 3. Deploy EC2-CORE"
    needs: [load-config, deploy-db]
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ needs.load-config.outputs.core-public }} >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Deploy with Docker Compose
        run: |
          # Transfer build context (source code) via tar + SSH
          echo "📦 Transferring CORE source context..."
          tar czf - micro-auth micro-estudiantes micro-maestros | ssh -i ~/.ssh/id_rsa ubuntu@${{ needs.load-config.outputs.core-public }} "cat > /tmp/core-context.tar.gz"
          
          # Transfer docker-compose file
          scp -i ~/.ssh/id_rsa docker-compose.ec2-core.yml ubuntu@${{ needs.load-config.outputs.core-public }}:/tmp/docker-compose.core.yml
          
          DOCKER_USER="${{ secrets.DOCKER_USERNAME }}"
          DOCKER_PASS="${{ secrets.DOCKER_TOKEN }}"
          
          ssh -i ~/.ssh/id_rsa ubuntu@${{ needs.load-config.outputs.core-public }} bash << DEPLOY
          set -e
          export DOCKER_USER="$DOCKER_USER"
          export DOCKER_PASS="$DOCKER_PASS"
          echo "================================"
          echo "🐳 EC2-CORE Deployment"
          echo "================================"
          
          # Extract build context
          echo "📂 Extracting CORE source context..."
          cd /tmp
          tar xzf core-context.tar.gz
          
          # Instalar docker-compose si no existe
          if ! command -v docker-compose &> /dev/null; then
            echo "📦 Instalando docker-compose..."
            sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-\$(uname -s)-\$(uname -m)" -o /usr/local/bin/docker-compose
            sudo chmod +x /usr/local/bin/docker-compose
            docker-compose --version
          fi
          
          cd /tmp
          docker-compose -f docker-compose.core.yml down 2>/dev/null || true
          docker-compose -f docker-compose.core.yml build --no-cache
          docker-compose -f docker-compose.core.yml up -d
          
          sleep 15
          docker-compose -f docker-compose.core.yml ps
          echo "✅ EC2-CORE deployment complete"
          DEPLOY

  # =========================================================================
  # 4. DEPLOY EC2-API-GATEWAY (Docker Compose)
  # =========================================================================
  deploy-api-gateway:
    name: "🌐 4. Deploy EC2-API-Gateway"
    needs: [load-config, deploy-core]
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ needs.load-config.outputs.api-gateway-public }} >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Deploy with Docker Compose
        run: |
          scp -i ~/.ssh/id_rsa docker-compose.api-gateway.yml ubuntu@${{ needs.load-config.outputs.api-gateway-public }}:/tmp/
          
          DOCKER_USER="${{ secrets.DOCKER_USERNAME }}"
          DOCKER_PASS="${{ secrets.DOCKER_TOKEN }}"
          
          ssh -i ~/.ssh/id_rsa ubuntu@${{ needs.load-config.outputs.api-gateway-public }} bash << DEPLOY
          set -e
          export DOCKER_USER="$DOCKER_USER"
          export DOCKER_PASS="$DOCKER_PASS"
          echo "================================"
          echo "🌐 EC2-API-Gateway Deployment"
          echo "================================"
          
          # Instalar docker-compose si no existe
          if ! command -v docker-compose &> /dev/null; then
            echo "📦 Instalando docker-compose..."
            sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-\$(uname -s)-\$(uname -m)" -o /usr/local/bin/docker-compose
            sudo chmod +x /usr/local/bin/docker-compose
            docker-compose --version
          fi
          
          # Login to Docker registry
          echo "\$DOCKER_PASS" | docker login -u "\$DOCKER_USER" --password-stdin
          
          cd /tmp
          docker-compose -f docker-compose.api-gateway.yml down 2>/dev/null || true
          docker-compose -f docker-compose.api-gateway.yml pull 2>&1 || true
          
          if [ "${{ github.event.inputs.rebuild_images }}" = "true" ]; then
            docker-compose -f docker-compose.api-gateway.yml build
          fi
          
          docker-compose -f docker-compose.api-gateway.yml up -d
          
          sleep 10
          docker-compose -f docker-compose.api-gateway.yml ps
          echo "✅ EC2-API-Gateway deployment complete"
          DEPLOY

  # =========================================================================
  # 5. DEPLOY EC2-REPORTES (Docker Compose)
  # =========================================================================
  deploy-reportes:
    name: "📄 5. Deploy EC2-Reportes"
    needs: [load-config, deploy-db]
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ needs.load-config.outputs.reportes-public }} >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Deploy with Docker Compose
        run: |
          # Verify directories exist
          echo "📁 Checking Reportes directories..."
          [ -d micro-reportes-estudiantes ] && echo "✅ micro-reportes-estudiantes found" || echo "❌ micro-reportes-estudiantes NOT found"
          [ -d micro-reportes-maestros ] && echo "✅ micro-reportes-maestros found" || echo "❌ micro-reportes-maestros NOT found"
          
          # Transfer docker-compose file
          scp -i ~/.ssh/id_rsa docker-compose.ec2-reportes.yml ubuntu@${{ needs.load-config.outputs.reportes-public }}:/tmp/docker-compose.reportes.yml
          
          # Create tar and pipe directly to remote instance
          echo "📦 Creating and transferring tar archive..."
          tar czf - micro-reportes-estudiantes micro-reportes-maestros | ssh -i ~/.ssh/id_rsa ubuntu@${{ needs.load-config.outputs.reportes-public }} "cat > /tmp/reportes-context.tar.gz && ls -lh /tmp/reportes-context.tar.gz"
          
          DOCKER_USER="${{ secrets.DOCKER_USERNAME }}"
          DOCKER_PASS="${{ secrets.DOCKER_TOKEN }}"
          
          ssh -i ~/.ssh/id_rsa ubuntu@${{ needs.load-config.outputs.reportes-public }} bash << DEPLOY
          set -e
          export DOCKER_USER="$DOCKER_USER"
          export DOCKER_PASS="$DOCKER_PASS"
          echo "================================"
          echo "📄 EC2-Reportes Deployment"
          echo "================================"
          
          # Instalar docker-compose si no existe
          if ! command -v docker-compose &> /dev/null; then
            echo "📦 Instalando docker-compose..."
            sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-\$(uname -s)-\$(uname -m)" -o /usr/local/bin/docker-compose
            sudo chmod +x /usr/local/bin/docker-compose
            docker-compose --version
          fi
          
          # Login to Docker registry
          echo "\$DOCKER_PASS" | docker login -u "\$DOCKER_USER" --password-stdin
          
          cd /tmp
          tar xzf reportes-context.tar.gz
          docker-compose -f docker-compose.reportes.yml down 2>/dev/null || true
          docker-compose -f docker-compose.reportes.yml build --no-cache
          docker-compose -f docker-compose.reportes.yml up -d
          
          sleep 10
          docker-compose -f docker-compose.reportes.yml ps
          echo "✅ EC2-Reportes deployment complete"
          DEPLOY

  # =========================================================================
  # 6. DEPLOY EC2-NOTIFICACIONES (Docker Compose)
  # =========================================================================
  deploy-notificaciones:
    name: "🔔 6. Deploy EC2-Notificaciones"
    needs: [load-config, deploy-db, deploy-messaging]
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ needs.load-config.outputs.notificaciones-public }} >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Deploy with Docker Compose
        run: |
          # Verify directory exists
          echo "📁 Checking Notificaciones directory..."
          [ -d micro-notificaciones ] && echo "✅ micro-notificaciones found" || echo "❌ micro-notificaciones NOT found"
          
          # Transfer docker-compose file
          scp -i ~/.ssh/id_rsa docker-compose.ec2-notificaciones.yml ubuntu@${{ needs.load-config.outputs.notificaciones-public }}:/tmp/docker-compose.notificaciones.yml
          
          # Create tar and pipe directly to remote instance
          echo "📦 Creating and transferring tar archive..."
          tar czf - micro-notificaciones | ssh -i ~/.ssh/id_rsa ubuntu@${{ needs.load-config.outputs.notificaciones-public }} "cat > /tmp/notificaciones-context.tar.gz && ls -lh /tmp/notificaciones-context.tar.gz"
          
          DOCKER_USER="${{ secrets.DOCKER_USERNAME }}"
          DOCKER_PASS="${{ secrets.DOCKER_TOKEN }}"
          
          ssh -i ~/.ssh/id_rsa ubuntu@${{ needs.load-config.outputs.notificaciones-public }} bash << DEPLOY
          set -e
          export DOCKER_USER="$DOCKER_USER"
          export DOCKER_PASS="$DOCKER_PASS"
          echo "================================"
          echo "🔔 EC2-Notificaciones Deployment"
          echo "================================"
          
          # Instalar docker-compose si no existe
          if ! command -v docker-compose &> /dev/null; then
            echo "📦 Instalando docker-compose..."
            sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-\$(uname -s)-\$(uname -m)" -o /usr/local/bin/docker-compose
            sudo chmod +x /usr/local/bin/docker-compose
            docker-compose --version
          fi
          
          # Login to Docker registry
          echo "\$DOCKER_PASS" | docker login -u "\$DOCKER_USER" --password-stdin
          
          cd /tmp
          tar xzf notificaciones-context.tar.gz
          docker-compose -f docker-compose.notificaciones.yml down 2>/dev/null || true
          docker-compose -f docker-compose.notificaciones.yml pull 2>&1 || true
          docker-compose -f docker-compose.notificaciones.yml build
          docker-compose -f docker-compose.notificaciones.yml up -d
          
          sleep 10
          docker-compose -f docker-compose.notificaciones.yml ps
          echo "✅ EC2-Notificaciones deployment complete"
          DEPLOY

  # =========================================================================
  # 7. DEPLOY EC2-ANALYTICS (Docker Compose)
  # =========================================================================
  deploy-analytics:
    name: "📊 7. Deploy EC2-Analytics"
    needs: [load-config, deploy-db]
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ needs.load-config.outputs.analytics-public }} >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Deploy with Docker Compose
        run: |
          scp -i ~/.ssh/id_rsa docker-compose.ec2-analytics.yml ubuntu@${{ needs.load-config.outputs.analytics-public }}:/tmp/docker-compose.analytics.yml
          
          DOCKER_USER="${{ secrets.DOCKER_USERNAME }}"
          DOCKER_PASS="${{ secrets.DOCKER_TOKEN }}"
          
          ssh -i ~/.ssh/id_rsa ubuntu@${{ needs.load-config.outputs.analytics-public }} bash << DEPLOY
          set -e
          export DOCKER_USER="$DOCKER_USER"
          export DOCKER_PASS="$DOCKER_PASS"
          echo "================================"
          echo "📊 EC2-Analytics Deployment"
          echo "================================"
          
          # Instalar docker-compose si no existe
          if ! command -v docker-compose &> /dev/null; then
            echo "📦 Instalando docker-compose..."
            sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-\$(uname -s)-\$(uname -m)" -o /usr/local/bin/docker-compose
            sudo chmod +x /usr/local/bin/docker-compose
            docker-compose --version
          fi
          
          # Login to Docker registry
          echo "\$DOCKER_PASS" | docker login -u "\$DOCKER_USER" --password-stdin
          
          cd /tmp
          docker-compose -f docker-compose.analytics.yml down 2>/dev/null || true
          docker-compose -f docker-compose.analytics.yml pull 2>&1 || true
          
          if [ "${{ github.event.inputs.rebuild_images }}" = "true" ]; then
            docker-compose -f docker-compose.analytics.yml build
          fi
          
          docker-compose -f docker-compose.analytics.yml up -d
          
          sleep 10
          docker-compose -f docker-compose.analytics.yml ps
          echo "✅ EC2-Analytics deployment complete"
          DEPLOY

  # =========================================================================
  # 8. DEPLOY EC2-MONITORING (Docker Compose)
  # =========================================================================
  deploy-monitoring:
    name: "📈 8. Deploy EC2-Monitoring"
    needs: [load-config, deploy-core]
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ needs.load-config.outputs.monitoring-public }} >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Deploy with Docker Compose
        run: |
          scp -i ~/.ssh/id_rsa docker-compose.ec2-monitoring.yml ubuntu@${{ needs.load-config.outputs.monitoring-public }}:/tmp/docker-compose.monitoring.yml
          
          DOCKER_USER="${{ secrets.DOCKER_USERNAME }}"
          DOCKER_PASS="${{ secrets.DOCKER_TOKEN }}"
          
          ssh -i ~/.ssh/id_rsa ubuntu@${{ needs.load-config.outputs.monitoring-public }} bash << DEPLOY
          set -e
          export DOCKER_USER="$DOCKER_USER"
          export DOCKER_PASS="$DOCKER_PASS"
          echo "================================"
          echo "📈 EC2-Monitoring Deployment"
          echo "================================"
          
          # Instalar docker-compose si no existe
          if ! command -v docker-compose &> /dev/null; then
            echo "📦 Instalando docker-compose..."
            sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-\$(uname -s)-\$(uname -m)" -o /usr/local/bin/docker-compose
            sudo chmod +x /usr/local/bin/docker-compose
            docker-compose --version
          fi
          
          # Login to Docker registry
          echo "\$DOCKER_PASS" | docker login -u "\$DOCKER_USER" --password-stdin
          
          cd /tmp
          docker-compose -f docker-compose.monitoring.yml down 2>/dev/null || true
          docker-compose -f docker-compose.monitoring.yml pull 2>&1 || true
          
          if [ "${{ github.event.inputs.rebuild_images }}" = "true" ]; then
            docker-compose -f docker-compose.monitoring.yml build
          fi
          
          docker-compose -f docker-compose.monitoring.yml up -d
          
          sleep 10
          docker-compose -f docker-compose.monitoring.yml ps
          echo "✅ EC2-Monitoring deployment complete"
          DEPLOY

  # =========================================================================
  # 9. DEPLOY EC2-FRONTEND (Docker Compose)
  # =========================================================================
  deploy-frontend:
    name: "🎨 9. Deploy EC2-Frontend"
    needs: [load-config, deploy-api-gateway]
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ needs.load-config.outputs.frontend-public }} >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Deploy with Docker Compose
        run: |
          scp -i ~/.ssh/id_rsa docker-compose.frontend.yml ubuntu@${{ needs.load-config.outputs.frontend-public }}:/tmp/
          
          DOCKER_USER="${{ secrets.DOCKER_USERNAME }}"
          DOCKER_PASS="${{ secrets.DOCKER_TOKEN }}"
          
          ssh -i ~/.ssh/id_rsa ubuntu@${{ needs.load-config.outputs.frontend-public }} bash << DEPLOY
          set -e
          export DOCKER_USER="$DOCKER_USER"
          export DOCKER_PASS="$DOCKER_PASS"
          echo "================================"
          echo "🎨 EC2-Frontend Deployment"
          echo "================================"
          
          # Instalar docker-compose si no existe
          if ! command -v docker-compose &> /dev/null; then
            echo "📦 Instalando docker-compose..."
            sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-\$(uname -s)-\$(uname -m)" -o /usr/local/bin/docker-compose
            sudo chmod +x /usr/local/bin/docker-compose
            docker-compose --version
          fi
          
          # Login to Docker registry
          echo "\$DOCKER_PASS" | docker login -u "\$DOCKER_USER" --password-stdin
          
          cd /tmp
          docker-compose -f docker-compose.frontend.yml down 2>/dev/null || true
          docker-compose -f docker-compose.frontend.yml pull 2>&1 || true
          
          if [ "${{ github.event.inputs.rebuild_images }}" = "true" ]; then
            docker-compose -f docker-compose.frontend.yml build
          fi
          
          docker-compose -f docker-compose.frontend.yml up -d
          
          sleep 10
          docker-compose -f docker-compose.frontend.yml ps
          echo "✅ EC2-Frontend deployment complete"
          DEPLOY

  # =========================================================================
  # 10. DEPLOY EC-BASTION (Docker Compose)
  # =========================================================================
  deploy-bastion:
    name: "🛡️  10. Deploy EC-Bastion"
    needs: [load-config, deploy-frontend]
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ needs.load-config.outputs.bastion-public }} >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Deploy with Docker Compose
        run: |
          scp -i ~/.ssh/id_rsa docker-compose.ec2-bastion.yml ubuntu@${{ needs.load-config.outputs.bastion-public }}:/tmp/docker-compose.bastion.yml
          
          DOCKER_USER="${{ secrets.DOCKER_USERNAME }}"
          DOCKER_PASS="${{ secrets.DOCKER_TOKEN }}"
          
          ssh -i ~/.ssh/id_rsa ubuntu@${{ needs.load-config.outputs.bastion-public }} bash << DEPLOY
          set -e
          export DOCKER_USER="$DOCKER_USER"
          export DOCKER_PASS="$DOCKER_PASS"
          echo "================================"
          echo "🛡️  EC-Bastion Deployment"
          echo "================================"
          
          # Instalar docker-compose si no existe
          if ! command -v docker-compose &> /dev/null; then
            echo "📦 Instalando docker-compose..."
            sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-\$(uname -s)-\$(uname -m)" -o /usr/local/bin/docker-compose
            sudo chmod +x /usr/local/bin/docker-compose
            docker-compose --version
          fi
          
          # Login to Docker registry
          echo "\$DOCKER_PASS" | docker login -u "\$DOCKER_USER" --password-stdin
          
          cd /tmp
          docker-compose -f docker-compose.bastion.yml down 2>/dev/null || true
          docker-compose -f docker-compose.bastion.yml pull 2>&1 || true
          
          if [ "${{ github.event.inputs.rebuild_images }}" = "true" ]; then
            docker-compose -f docker-compose.bastion.yml build
          fi
          
          docker-compose -f docker-compose.bastion.yml up -d
          
          sleep 5
          docker-compose -f docker-compose.bastion.yml ps
          echo "✅ EC-Bastion deployment complete"
          DEPLOY

  # =========================================================================
  # FINAL VERIFICATION & SUMMARY
  # =========================================================================
  verification:
    name: "✅ Final Verification"
    needs: [load-config, deploy-bastion]
    runs-on: ubuntu-latest
    if: success()

    steps:
      - name: Print deployment summary
        run: |
          echo ""
          echo "╔════════════════════════════════════════════════════════════════════╗"
          echo "║                                                                    ║"
          echo "║    ✅ DEPLOYMENT COMPLETE - ALL 10 INSTANCES DEPLOYED!            ║"
          echo "║                                                                    ║"
          echo "║           Using Docker Compose (Optimized Deployment)             ║"
          echo "║                                                                    ║"
          echo "╚════════════════════════════════════════════════════════════════════╝"
          echo ""
          echo "📊 DEPLOYED SERVICES:"
          echo "   ✅ EC2-Messaging:        docker-compose.messaging.yml"
          echo "   ✅ EC2-DB:               docker-compose.db.yml"
          echo "   ✅ EC2-CORE:             docker-compose.core.yml"
          echo "   ✅ EC2-API-Gateway:      docker-compose.api-gateway.yml"
          echo "   ✅ EC2-Reportes:         docker-compose.reportes.yml"
          echo "   ✅ EC2-Notificaciones:   docker-compose.notificaciones.yml"
          echo "   ✅ EC2-Analytics:        docker-compose.analytics.yml"
          echo "   ✅ EC2-Monitoring:       docker-compose.monitoring.yml"
          echo "   ✅ EC2-Frontend:         docker-compose.frontend.yml"
          echo "   ✅ EC-Bastion:           docker-compose.bastion.yml"
          echo ""
          echo "🎯 BENEFITS:"
          echo "   ✓ Cleaner deployment (using docker-compose)"
          echo "   ✓ Easier to maintain and update services"
          echo "   ✓ Version controlled docker-compose files"
          echo "   ✓ Faster deployments"
          echo "   ✓ Consistent with local development"
          echo ""
          echo "📍 SERVICE URLs:"
          echo "   🎨 Frontend:      http://${{ needs.load-config.outputs.frontend-public }}:5500"
          echo "   🌐 API Gateway:   http://${{ needs.load-config.outputs.api-gateway-public }}:8080"
          echo "   📈 Grafana:       http://${{ needs.load-config.outputs.monitoring-public }}:3000"
          echo "   📊 Prometheus:    http://${{ needs.load-config.outputs.monitoring-public }}:9090"
          echo ""
