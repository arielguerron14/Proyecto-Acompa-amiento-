name: Deploy Micro Reportes Maestros

on:
  push:
    branches: [ main ]
    paths:
      - micro-reportes-maestros/**
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Inspect repo (debug)
        run: |
          echo "PWD: $(pwd)"
          ls -la

      - name: Build & Push
        run: |
          # Use repo root as build context and Dockerfile inside micro-reportes-maestros
          docker build -t caguerronp/proyecto-acomp-micro-reportes-maestros:latest -f micro-reportes-maestros/Dockerfile .
          docker push caguerronp/proyecto-acomp-micro-reportes-maestros:latest

      - name: Deploy on EC2-Reportes
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_REPORTES_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_KEY }}
          script: |
            docker pull caguerronp/proyecto-acomp-micro-reportes-maestros:latest
            docker stop micro-reportes-maestros || true
            docker rm micro-reportes-maestros || true
            docker run -d \
              --name micro-reportes-maestros \
              -p 4002:8080 \
              --restart unless-stopped \
              caguerronp/proyecto-acomp-micro-reportes-maestros:latest
