name: Update AWS Instance IPs

on:
  workflow_dispatch:

jobs:
  update-ips:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Python dependencies
        run: |
          pip install pyyaml boto3 -q

      - name: Configure AWS credentials and fetch instance IPs
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_SESSION_TOKEN: ${{ secrets.AWS_SESSION_TOKEN }}
          AWS_DEFAULT_REGION: us-east-1
        run: |
          echo "🔄 Fetching instance IPs from AWS using credentials..."
          python3 << 'EOF'
          import json
          import boto3
          import os
          import sys

          try:
              # Usar credenciales de entorno
              session = boto3.Session(
                  aws_access_key_id=os.environ.get('AWS_ACCESS_KEY_ID'),
                  aws_secret_access_key=os.environ.get('AWS_SECRET_ACCESS_KEY'),
                  aws_session_token=os.environ.get('AWS_SESSION_TOKEN'),
                  region_name=os.environ.get('AWS_DEFAULT_REGION', 'us-east-1')
              )
              
              ec2 = session.client('ec2')
              response = ec2.describe_instances(
                  Filters=[{'Name': 'instance-state-name', 'Values': ['running']}]
              )
              
              instances = {}
              
              print("📋 Found instances from AWS:")
              for reservation in response.get('Reservations', []):
                  for instance in reservation.get('Instances', []):
                      instance_id = instance['InstanceId']
                      instance_name = None
                      
                      # Get name from tags
                      for tag in instance.get('Tags', []):
                          if tag['Key'] == 'Name':
                              instance_name = tag['Value']
                              break
                      
                      if instance_name:
                          public_ip = instance.get('PublicIpAddress', 'N/A')
                          private_ip = instance.get('PrivateIpAddress', 'N/A')
                          instance_type = instance.get('InstanceType', 'unknown')
                          
                          instances[instance_name] = {
                              'InstanceId': instance_id,
                              'Name': instance_name,
                              'PublicIpAddress': public_ip,
                              'PrivateIpAddress': private_ip,
                              'Type': instance_type
                          }
                          
                          print(f"  ✅ {instance_name}")
                          print(f"      ID: {instance_id}")
                          print(f"      Public IP: {public_ip}")
                          print(f"      Private IP: {private_ip}")

              if instances:
                  # Update config/instance_ips.json
                  with open('config/instance_ips.json', 'w') as f:
                      json.dump(instances, f, indent=4)
                  
                  print(f"\n✅ Updated config/instance_ips.json with {len(instances)} instances")
              else:
                  print("⚠️  No instances found, skipping update")
                  
          except Exception as e:
              print(f"❌ Error: {e}")
              import traceback
              traceback.print_exc()
              sys.exit(1)  # Fail the workflow if credentials are missing
          EOF

      - name: Sync IPs to configuration files
        run: |
          echo "🔄 Syncing instance IPs to all configuration files..."
          python3 sync-ips-to-config.py
          echo "✅ IP synchronization complete"

      - name: Sync IPs to .env files
        run: |
          echo "🔄 Syncing IPs to .env files..."
          python3 sync-ips-to-env.py
          echo "✅ .env files updated"

      - name: Commit and push changes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # Check for changes
          if git diff --quiet; then
            echo "✅ No changes to commit"
            exit 0
          fi
          
          # Stage changed files
          git add config/ api-gateway/ micro-auth/ micro-estudiantes/ micro-maestros/ micro-notificaciones/ micro-messaging/ monitoring/ frontend-web/ 2>/dev/null || true
          
          # Commit only if there are staged changes
          if git diff --cached --quiet; then
            echo "✅ No changes to commit"
          else
            git commit -m "chore: Update instance IPs and sync all configs [automated]"
            
            # Pull before push to avoid conflicts
            echo "📥 Pulling latest changes..."
            git pull --rebase origin main || true
            
            # Push with retry
            echo "📤 Pushing changes..."
            for i in {1..3}; do
              if git push origin main; then
                echo "✅ Push successful"
                exit 0
              else
                echo "⏳ Push attempt $i failed, retrying..."
                sleep 5
              fi
            done
            echo "⚠️ Push failed after 3 attempts, but configuration synced"
          fi