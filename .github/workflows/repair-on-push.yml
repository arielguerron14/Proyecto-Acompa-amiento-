name: REMOVED: Repair on push (run-repair)

# Removed from active workflows per request; archived at .github/disabled-workflows/repair-on-push.yml
on: []

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Removed
        run: echo "Workflow removed from active workflows; archived at .github/disabled-workflows/repair-on-push.yml"
